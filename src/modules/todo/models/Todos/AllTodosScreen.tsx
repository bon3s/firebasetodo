import React from 'react';
import { RouterProps } from "react-router"

interface Props extends RouterProps {}
const AllTodosScreen = (props:Props) =>{
    return <div>all todos list</div>
}

export default AllTodosScreen;